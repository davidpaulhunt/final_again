.container
  .row
    %br
    .col-md-2
    .col-md-8
      .col-md-4
        %h1= @player.first_name + " " + @player.last_name
        = image_tag "#{@player.account.avatar.present? ? @player.account.avatar.url(:web, :preview) : 'player1.jpeg'}"
      .col-md-4
        %ul
          - @player.positions.each do |position|
            %li= position.abbreviation
    .col-md-2
    %br
  .row
    .col-md-2
    .col-md-8
      %br
      %br
      %legend Player Statistics
      %table
        %theader
          %th
            Year
          %th
            Passing Yards
        %tbody
        - @player.stats.each do |stat|
          %tr
            %td= stat.year
          %tr
            %td= stat.passing_yards
    .col-md-2